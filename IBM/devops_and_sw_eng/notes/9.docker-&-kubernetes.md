## Introduction to Containers w/ Docker, Kubernetes & OpenShift

### Containers and Containerization

- A container is a unit of software that encapsulates everything needed to build, ship, and run applications.
- Containers lower deployment time and costs, improve utilization, automate processes, and support next-gen applications (microservices). Major container vendors include Docker, Podman, LXC, and Vagrant.
- Docker is an open platform used for developing, shipping, and running applications as containers.
- Docker containers are not a good fit for applications based on monolithic architecture or applications that require high performance or security.
- Docker architecture consists of the Docker client, the Docker host, and the container registry.
- The Docker host contains objects such as the Dockerfiles, images, containers, networks, storage volumes, and other objects, such as plugins and add-ons.
- Docker uses networks to isolate container communications.
- Docker uses volumes and binds mounts to persist data even after a container stops running.
- Plugins, such as storage plugins, provide the ability to connect to external storage platforms.

#### Cheat Sheet: Docker CLI

| Command | Description |
|----------|-------------|
| curl localhost | Pings the application. |
| docker build | Builds an image from a Dockerfile. |
| docker build . -t | Builds the image and tags the image id. |
| docker container rm | Removes a container. |
| docker images | Lists the images. |
| docker ps | Lists the containers. |
| docker ps -a | Lists the containers that ran and exited successfully. |
| docker pull | Pulls the latest image or repository from a registry. |
| docker push | Pushes an image or a repository to a registry. |
| docker run | Runs a command in a new container. |
| docker run -p | Runs the container by publishing the ports. |
| docker stop | Stops one or more running containers. |
| docker stop $(docker ps -q) | Stops all running containers. |
| docker tag | Creates a tag for a target image that refers to a source image. |
| docker --version | Displays the version of the Docker CLI. |
| exit | Closes the terminal session. |
| export MY_NAMESPACE | Exports a namespace as an environment variable. |
| git clone | Clones the git repository that contains the artifacts needed. |
| ibmcloud cr images | Lists images in the IBM Cloud Container Registry. |
| ibmcloud cr login | Logs your local Docker daemon into IBM Cloud Container Registry. |
| ibmcloud cr namespaces | Views the namespaces you have access to. |
| ibmcloud cr region-set | Ensures that you are targeting the region appropriate to your cloud account. |
| ibmcloud target | Provides information about the account you’re targeting. |
| ibmcloud version | Displays the version of the IBM Cloud CLI. |
| ls | Lists the contents of this directory to see the artifacts. |

#### Glossary: Container Basics

| Term | Definition |
|------|-------------|
| Agile | is an iterative approach to project management and software development that helps teams deliver value to their customers faster and with fewer issues. |
| Client-server architecture | is a distributed application structure that partitions tasks or workloads between the providers of a resource or service, called servers, and service requesters, called clients. |
| A container | powered by the containerization engine, is a standard unit of software that encapsulates the application code, runtime, system tools, system libraries, and settings necessary for programmers to efficiently build, ship and run applications. |
| Container Registry | Used for the storage and distribution of named container images. While many features can be built on top of a registry, its most basic functions are to store images and retrieve them. |
| CI/CD pipelines | A continuous integration and continuous deployment (CI/CD) pipeline is a series of steps that must be performed in order to deliver a new version of software. CI/CD pipelines are a practice focused on improving software delivery throughout the software development life cycle via automation. |
| Cloud native | A cloud-native application is a program that is designed for a cloud computing architecture. These applications are run and hosted in the cloud and are designed to capitalize on the inherent characteristics of a cloud computing software delivery model. |
| Daemon-less | A container runtime that does not run any specific program (daemon) to create objects, such as images, containers, networks, and volumes. |
| DevOps | is a set of practices, tools, and a cultural philosophy that automate and integrate the processes between software development and IT teams. |
| Docker | An open container platform for developing, shipping and running applications in containers. |
| A Dockerfile | is a text document that contains all the commands you would normally execute manually in order to build a Docker image. Docker can build images automatically by reading the instructions from a Dockerfile. |
| Docker client | is the primary way that many Docker users interact with Docker. When you use commands such as docker run, the client sends these commands to dockerd, which carries them out. The docker command uses the Docker API. The Docker client can communicate with more than one daemon. |
| Docker Command Line Interface (CLI) | The Docker client provides a command line interface (CLI) that allows you to issue build, run, and stop application commands to a Docker daemon. |
| Docker daemon (dockerd) | creates and manages Docker objects, such as images, containers, networks, and volumes. |
| Docker Hub | is the world's easiest way to create, manage, and deliver your team's container applications. |
| Docker localhost | Docker provides a host network which lets containers share your host's networking stack. This approach means that a localhost in a container resolves to the physical host, instead of the container itself. |
| Docker remote host | A remote Docker host is a machine, inside or outside our local network which is running a Docker Engine and has ports exposed for querying the Engine API. |
| Docker networks | help isolate container communications. |
| Docker plugins | such as a storage plugin, provides the ability to connect external storage platforms. |
| Docker storage | uses volumes and bind mounts to persist data even after a running container is stopped. |
| LXC | LinuX Containers is an OS-level virtualization technology that allows creation and running of multiple isolated Linux virtual environments (VE) on a single control host. |
| IBM Cloud Container Registry | stores and distributes container images in a fully managed private registry. |
| Image | An immutable file that contains the source code, libraries, and dependencies that are necessary for an application to run. Images are templates or blueprints for a container. |
| Immutability | Images are read-only; if you change an image, you create a new image. |
| Microservices | are a cloud-native architectural approach in which a single application contains many loosely coupled and independently deployable smaller components or services. |
| Namespace | A Linux namespace is a Linux kernel feature that isolates and virtualizes system resources. Processes which are restricted to a namespace can only interact with resources or processes that are part of the same namespace. Namespaces are an important part of Docker's isolation model. Namespaces exist for each type of resource, including networking, storage, processes, hostname control and others. |
| Operating System Virtualization | OS-level virtualization is an operating system paradigm in which the kernel allows the existence of multiple isolated user space instances, called containers, zones, virtual private servers, partitions, virtual environments, virtual kernels, or jails. |
| Private Registry | Restricts access to images so that only authorized users can view and use them. |
| REST API | A REST API (also known as RESTful API) is an application programming interface (API or web API) that conforms to the constraints of REST architectural style and allows for interaction with RESTful web services. |
| Registry | is a hosted service containing repositories of images which responds to the Registry API. |
| Repository | is a set of Docker images. A repository can be shared by pushing it to a registry server. The different images in the repository can be labelled using tags. |
| Server Virtualization | Server virtualization is the process of dividing a physical server into multiple unique and isolated virtual servers by means of a software application. Each virtual server can run its own operating systems independently. |
| Serverless | is a cloud-native development model that allows developers to build and run applications without having to manage servers. |
| Tag | A tag is a label applied to a Docker image in a repository. Tags are how various images in a repository are distinguished from each other. |

### Kubernetes Basics

- Container orchestration automates the container lifecycle resulting in faster deployments, reduced errors, higher availability, and more robust security.
- Kubernetes is a highly portable, horizontally scalable, open-source container orchestration system with automated deployment and simplified management capabilities.
- Kubernetes architecture consists of a control plane and one or more worker planes.
- A control plane includes controllers, an API server, a scheduler, and an etcd.
- A worker plane includes nodes, a kubelet, container runtime, and kube-proxy.
- Kubernetes objects include Namespaces, Pods, ReplicaSets, Deployments, and Services.
- Namespaces help in isolating groups of resources within a single cluster.
- Pods represent a process or an instance of an app running in the cluster.
- ReplicaSets create and manage horizontally scaled running Pods.
- Deployments provide updates for Pods and ReplicaSets.
- A service in Kubernetes is a REST object that provides policies for accessing the pods and cluster.
- Kubernetes capabilities include automated rollouts and rollbacks, storage orchestration, horizontal scaling, automated bin packing, secret and configuration management, Ipv4/Ipv6 dual-stack support, batch execution, self-healing, service discovery, load balancing, and extensible design.
- Services in Kubernetes are REST objects that provide policies for accessing the pods and cluster. ClusterIP provides Inter-service communication within the cluster; a NodePort Service creates and routes the incoming requests automatically to the ClusterIP Service; the External Load Balancer, or ELB, creates NodePort and ClusterIP Services automatically and External Name service represents external storage as well as enables Pods from different namespaces to talk to each other.
- Ingress is an API object that provides routing rules to manage external users' access to multiple services in a Kubernetes cluster; whereas using a DaemonSet ensures that there is at least one copy of the pod on all nodes; a StatefulSet manages stateful applications, manages Pod deployment and scaling, maintains a sticky identity for each Pod request and provides persistent storage volumes for your workloads and lastly a Job creates pods and tracks the Pod completion process; Jobs are retried until completed.

#### Cheat Sheet: Understanding Kubernetes Architecture

| Command  | Description    |
|----------|----------------|
| for … do                     | Runs a for command multiple times as specified.                    |
| kubectl apply                | Applies a configuration to a resource.                             |
| kubectl config get-clusters  | Displays clusters defined in the kubeconfig.                       |
| kubectl config get-contexts  | Displays the current context.                                      |
| kubectl create               | Creates a resource.                                                |
| kubectl delete               | Deletes resources.                                                 |
| kubectl describe             | Shows details of a resource or group of resources.                |
| kubectl expose               | Exposes a resource to the internet as a Kubernetes service.       |
| kubectl get                  | Displays resources.                                                |
| kubectl get pods             | Lists all the Pods.                                                |
| kubectl get pods -o wide     | Lists all the Pods with additional details.                       |
| kubectl get deployments      | Lists the deployments created.                                     |
| kubectl get services         | Lists the services created.                                        |
| kubectl proxy                | Creates a proxy server between localhost and the Kubernetes API.   |
| kubectl run                  | Creates and runs a particular image in a Pod.                      |
| kubectl version              | Prints the client and server version information.                 |

#### Glossary: Kubernetes Basics

| Term                           | Definition |
|--------------------------------|------------|
| Automated bin packing          | Increases resource utilization and cost savings using a mix of critical and best-effort workloads. |
| Batch execution                | Manages batch and continuous integration workloads and automatically replaces failed containers, if configured. |
| Cloud Controller Manager       | A Kubernetes control plane component that embeds cloud-specific control logic and links your cluster to your cloud provider's API. |
| Cluster                        | A set of worker machines (nodes) that run containerized applications. Every cluster has at least one worker node. |
| Container Orchestration        | Automates the container lifecycle of containerized applications. |
| Container Runtime              | Software responsible for running containers. |
| Control Loop                   | A non-terminating loop that regulates the state of a system (like a thermostat). |
| Control plane                  | The orchestration layer that exposes APIs and manages container lifecycle. |
| Controller                     | Control loops that watch cluster state and move it toward the desired state. |
| Data (Worker) Plane            | Provides CPU, memory, network, storage — everything needed for containers to run. |
| DaemonSet                      | Ensures a copy of a Pod runs across a set of nodes in the cluster. |
| Declarative Management         | You declare the desired state; Kubernetes works to make the actual state match it. |
| Deployment                     | Manages updates for Pods and ReplicaSets; ideal for stateless applications. |
| Designed for extensibility     | Lets you add features to your cluster without modifying source code. |
| Docker Swarm                  | Docker-native container orchestration platform, popular for Docker-centric teams. |
| Ecosystem                      | Collection of tools, services, and support surrounding Kubernetes. |
| etcd                           | Highly available key-value store containing all cluster data; Kubernetes’s source of truth. |
| Eviction                       | Terminating Pods on nodes (e.g., due to resource pressure). |
| Imperative commands            | Directly create, update, or delete live objects. |
| Imperative Management          | Define step-by-step actions to reach a desired state. |
| Ingress                        | Manages external access to cluster services, typically HTTP. |
| IPv4/IPv6 dual stack           | Assigns both IPv4 and IPv6 addresses to Pods and Services. |
| Job                            | A task that runs to completion (batch, finite workloads). |
| Kubectl                        | Kubernetes CLI tool for interacting with the cluster API. |
| Kubelet                        | Node agent ensuring that containers described in PodSpecs are running and healthy. |
| Kubernetes                     | The de facto open-source container orchestration platform for deployment, scaling, storage, discovery, etc. |
| Kubernetes API                 | RESTful interface that exposes Kubernetes functionality and cluster state. |
| Kubernetes API Server          | Validates and configures API objects; the frontend to shared cluster state. |
| Kubernetes Controller Manager  | Runs controller processes to ensure actual state matches desired state. |
| Kubernetes Cloud Controller Manager | Control plane component with cloud-specific logic for integrating with cloud provider APIs. |
| Kubernetes Proxy               | Network proxy on each node, enabling communication to Pods and Services. |
| kube-scheduler                 | Assigns newly created Pods to suitable nodes. |
| Label Selector                 | Filters resources based on labels. |
| Labels                         | Key-value metadata tags attached to objects. |
| Load balancing                 | Distributes traffic across Pods for performance and high availability. |
| Marathon                       | Apache Mesos framework for managing container infrastructure at scale. |
| Namespace                      | Provides resource isolation within a single cluster. |
| Node                           | Worker machine (physical or virtual) that runs Pods. |
| Nomad                          | Hashicorp’s cluster manager and scheduler supporting many workloads across all infrastructures. |
| Object                         | An entity in Kubernetes representing some state of the cluster. |
| Persistence                    | Ensures an object exists until explicitly changed or removed. |
| Preemption                     | Evicts lower-priority Pods to schedule higher-priority ones. |
| Self-healing                   | Restarts, replaces, reschedules, and kills failing or unresponsive containers. |
| Service                        | Abstracts and exposes a set of Pods over a network endpoint. |
| Service Discovery              | Discovers Pods via IPs or DNS. |
| StatefulSet                    | Manages ordered, unique Pods; supports stateful applications. |
| Storage                        | Data stores supporting persistent or temporary Pod data. |
| Storage Orchestration          | Automatically mounts requested storage systems. |
| Pod                            | Smallest Kubernetes object; represents a running process and typically wraps one container. |
| Proxy                          | A server acting as an intermediary for another service. |
| ReplicaSet                     | Ensures a specified number of replica Pods are running. |
| Workload                       | Any application running on Kubernetes. |

### Managing Applications with Kubernetes

#### Deployment Strategies

To create a good strategy:

- Consider the product type and the target audience
- Shadow and canary strategies use live user requests, as opposed to using a sample of users.
- The A/B testing strategy is useful if the version of the application requires minor tweaks or UI feature changes.
- The blue/green strategy is useful if your version of the application is complex or critical and needs proper monitoring with no downtime during deployment.
- The canary strategy is a good choice if you want zero downtime and are comfortable exposing your version of the application to the public.
- A rolling strategy gradually deploys the new version of the application. There is no downtime, and it is easy to roll back.
- The recreate strategy is a good choice if the application is not critical and users aren’t impacted by a short downtime.

#### Autoscaling and Secrets Management

| Resource Field     | Definition                                      |
|--------------------|--------------------------------------------------|
| **Lower Bound**    | Minimum resources the VPA recommends.            |
| **Target**         | Optimal resources the VPA recommends.            |
| **Uncapped Target**| Target without any predefined limits.            |
| **Upper Bound**    | Maximum resources the VPA recommends.            |

#### Recommended Values

| Resource          | CPU     | Memory                       |
|-------------------|---------|-------------------------------|
| **Lower Bound**   | 25m     | 256MiB (262144KiB)            |
| **Target**        | 25m     | 256MiB                        |
| **Uncapped Target** | 25m   | 256MiB                        |
| **Upper Bound**   | 671m    | 1.34GiB (1438074878KiB)       |

#### Managing Applications with Kubernetes, more

- A ReplicaSet enables scaling by creating or deleting pods.
- A ReplicaSet always tries to match the actual state to the desired state.
- Autoscaling enables scaling as needed at the cluster or node level, and the pod level.
- Autoscaler types include horizontal pod (HPA), vertical pod (VPA), and cluster (CA).
- Rolling updates roll out app changes in a controlled and automated way.
- Rolling updates and rollback can be performed using all-at-once and one-at-a-time strategies.
- ConfigMaps are used to provide variables for your application.
- Secrets are used to provide sensitive information to your application.
- Binding an external Service to your deployment automatically provides the credentials to use the Service inside the code.
- Binding manages configuration and credentials for back-end Services while protecting sensitive data.

#### Cheat sheet

| Command          | Description               |
|------------------|---------------------------|
| `kubectl autoscale deployment`   | Autoscales a Kubernetes Deployment.                      |
| `kubectl create configmap`       | Creates a ConfigMap resource.                            |
| `kubectl get deployments -o wide`| Lists deployments with details.                          |
| `kubectl get hpa`                | Lists Horizontal Pod Autoscalers (HPA).                  |
| `kubectl scale deployment`       | Scales a deployment.                                     |
| `kubectl set image deployment`   | Updates the current deployment.                          |
| `kubectl rollout`                | Manages the rollout of a resource.                       |
| `kubectl rollout restart`        | Restarts the resource so that the containers restart.    |
| `kubectl rollout undo`           | Rolls back the resource.                                 |

#### Glossary

| Term     | Definition      |
|----------|-----------------|
| **Cluster Autoscaler**  | Also known as CA. An API resource that autoscales the cluster itself, increasing and decreasing the number of available nodes that pods can run on. |
| **Config Map**| An API object used to store non-confidential data in key-value pairs. Pods can consume ConfigMaps as environment variables, command-line arguments, or as configuration files in a volume.|
| **Horizontal Pod Autoscaler (HPA)**| An API resource that automatically scales the number of Pod replicas based on targeted CPU utilization or custom metric targets.|
| **Linguistic Analysis**| Detects the tone in a given text.|
| **IBM Cloud catalog**| Provides various services that range from visual recognition to natural language processing and creating chatbots.|
| **Persistent Volume (PV)**| An API object that represents a piece of storage in the cluster. Available as a general, pluggable resource that persists beyond the lifecycle of any individual Pod.|
| **Persistent Volume Claim (PVC)**| Claims storage resources defined in a PersistentVolume so that it can be mounted as a volume in a container.|
| **Rolling Updates**| Provide a way to roll out application changes in an automated and controlled fashion throughout your pods. Works with pod templates such as deployments, and allows rollback if something goes wrong.|
| **Secrets**| Stores sensitive information, such as passwords, OAuth tokens, and SSH keys.|
| **Service Binding**| The process required to consume external services or backing services (REST APIs, databases, event buses) in your applications.|
| **Tone Analyzer Service**| An IBM Cloud service used to illustrate service binding. It uses linguistic analysis to detect tone in a given text.|
| **Vertical Pod Autoscaler (VPA)**| An API resource that adds resources to an existing machine. A VPA lets you scale a service vertically within a cluster.|
| **Volume**| A directory containing data, accessible to multiple containers in a Pod.|
| **Volume Mount**| The act of mounting a declared volume into a container in the same Pod.|
| **Volume Plugin**| A plugin that enables integration of storage within a Pod.|

### The Kubernetes Ecosystem: OpenShift, Istio, etc

- OpenShift® is an enterprise-ready Kubernetes container platform built for open hybrid cloud.
- OpenShift is easier to use, integrates with Jenkins, and has more services and features.
- Custom resource definitions (CRDs) extend the Kubernetes API.
- CRDs paired with custom controllers create new, declarative APIs in Kubernetes.
- Operators use CRDs and custom controllers to automate cluster tasks.
- A build is a process that transforms inputs into an object.
- An ImageStream is an abstraction for referencing container images in OpenShift.
- A service mesh provides traffic management to control the flow of traffic between services, security to encrypt traffic between services, and observability of service behavior to troubleshoot and optimize applications.
- Istio is a service mesh that supports four concepts of connection, security, enforcement, and observability. It is commonly used with Microservices applications.
- Istio provides service communication metrics for basic service monitoring needs: latency, traffic, errors, and saturation.

#### Glossary

| Term                          | Definition |
|-------------------------------|------------|
| **A/B testing** | Strategy used mainly for testing new features in front-end applications. Evaluates two versions (A and B) to determine which performs better based on user interaction and feedback. |
| **Build** | The process of transforming inputs into a resultant object. |
| **BuildConfig** | An OpenShift-specific object defining the build process, including input sources and strategy. The BuildConfig is the blueprint; the build is an instance of it. |
| **Canary Deployments** | Gradually deploys a new application version to increasing numbers of users. Real user traffic helps detect bugs before a global rollout. |
| **Circuit breaking** | A method to prevent errors in one microservice from cascading into others. |
| **Configuration Change** | A trigger that runs a new build when a new BuildConfig resource is created. |
| **Control Plane** | Takes desired configuration and dynamically programs proxy servers as the environment changes. |
| **Custom build strategy** | Requires defining and creating your own builder image. |
| **Custom builder images** | Regular Docker images containing the logic to transform inputs into outputs. |
| **CRDs** | Custom code defining new Kubernetes API resources without building a full custom API server. |
| **Custom controllers** | Reconcile the actual state of custom resources (CRDs) with their desired state. |
| **Data plane** | Handles communication between services. Without a service mesh, the network cannot identify traffic type, source, or destination for decision-making. |
| **Enforceability (Control)** | Istio enforces policies across the fleet and ensures fair resource distribution among consumers. |
| **Envoy proxy** | A proxy used by service meshes (like Istio) to intercept all network traffic and enable advanced features. |
| **Human operators** | Understand systems, deploy services, detect problems, and fix issues manually. |
| **Image Change** | A trigger to rebuild applications when a new or updated image becomes available. |
| **ImageStream** | An OpenShift abstraction for referencing container images. Acts as pointers to image digests. |
| **ImageStream Tag** | Identifies an ImageStream pointer that references a specific image. |
| **Istio** | A platform-independent service mesh controlling traffic flow, securing services, providing observability, and managing policies. |
| **Man-in-the-middle attacks** | A cyber-attack where communication between two parties is intercepted and controlled by an attacker. |
| **Observability** | Ability to trace call flows, dependencies, and metrics (latency, errors) within a mesh. |
| **OpenShift** | A hybrid cloud enterprise Kubernetes platform. |
| **OpenShift CI/CD process** | Automatically merges code, builds, tests, approves, and deploys new versions across environments. |
| **Operators** | Automate cluster tasks and act as custom controllers to extend the Kubernetes API. |
| **Operator Framework** | A suite of tools for building, testing, delivering, and updating Operators. |
| **OperatorHub** | Web console for discovering Operators available for installation on the cluster. |
| **Operator Lifecycle Manager (OLM)** | Controls installation, upgrades, and RBAC of Operators. |
| **Operator maturity model** | Defines Operator maturity levels, from Basic Install to Auto Pilot. |
| **Operator Pattern** | A system design linking a Controller to one or more custom resources. |
| **Operator Registry** | Stores CRDs, CSVs, and Operator metadata, providing catalog data to OLM. |
| **Operator SDK** | Tools (Helm, Go, Ansible) to build, test, and package Operators without deep API knowledge. |
| **postCommit** | BuildConfig section defining an optional build hook. |
| **Retries** | Mechanism allowing microservices to reattempt failed requests. |
| **runPolicy** | Field defining how builds from a BuildConfig should run (e.g., Serial or parallel). |
| **Service Broker** | Provides short-running processes that cannot handle long-term operations like upgrades or scaling. |
| **Service Mesh** | A dedicated layer making service-to-service communication secure, reliable, observable, and controllable. |
| **Software operators** | Capture the expertise of human operators and automate those processes. |
| **Source-to-Image (S2i)** | Tool for building reproducible images by injecting source code into a base image. |
| **Source strategy** | Section defining what build strategy is used (Source, Docker, Custom). |
| **Source type** | Determines primary input source (Git repo, inline Dockerfile, binary payload). |
| **Webhook** | A trigger that calls an OpenShift API endpoint (often from GitHub) when new code is committed or merged, enabling automated build flows. |

### Docker & Cloud Native Basics

| Term | Definition |
|------|------------|
| Agile | An iterative approach to project management and software development that helps teams deliver value faster and with fewer issues. |
| Client-server architecture | A distributed application structure dividing workloads between servers (providers) and clients (users). |
| Container | A standard software unit that packages code, runtime, libraries, and settings for reliable deployment. |
| Container Registry | A system for storing and distributing container images. |
| CI/CD pipelines | Automated steps that streamline delivering new software versions with improved efficiency and quality. |
| Cloud native | Applications built specifically for cloud environments, leveraging cloud delivery models. |
| Daemon-less | A container runtime that works without a background daemon managing images, networks, and containers. |
| DevOps | Practices and tools enabling collaboration and automation between development and IT operations. |
| Docker | An open container platform for building, shipping, and running applications. |
| Dockerfile | A configuration file containing instructions for creating a Docker image. |
| Docker client | The primary user interface that sends commands to the Docker daemon. |
| Docker CLI | Command-line interface for interacting with Docker via dockerd. |
| Docker daemon (dockerd) | The core process handling images, networks, volumes, and container lifecycle. |
| Docker Hub | Cloud-based registry for storing, managing, sharing, and deploying Docker images. |
| Docker localhost | Host network mode where container `localhost` refers to the physical host. |
| Docker remote host | A Docker Engine accessible over the network via the Docker API. |
| Docker networks | Logical networking that isolates container communication. |
| Docker plugins | Extensions that add functionality such as storage, networking, or logging. |
| Docker storage | Data persistence using volumes or bind mounts. |
| IBM Cloud Container Registry | A managed private registry for securely storing container images. |
| Image | A static template containing everything needed to run a container. |
| Immutability | Images are read-only; modifications create new images. |
| LXC | Linux Containers — OS-level virtualization enabling isolated environments. |
| Microservices | Architecture of small, loosely coupled, independently deployable services. |
| Namespace | Kernel feature isolating resources such as processes, networking, and storage. |
| OS Virtualization | Kernel-level virtualization supporting isolated user-space instances. |
| Private Registry | Registry limiting image access to authorized users. |
| REST API | API conforming to REST principles used for interacting with web services. |
| Registry | A hosted service storing and serving image repositories. |
| Repository | A collection of versioned container images. |
| Server Virtualization | Dividing a physical server into isolated virtual machines. |
| Serverless | Development model with automatic infrastructure handling—no server management required. |
| Tag | A label referencing a specific image version. |

### Kubernetes Basics

| Term | Definition |
|------|------------|
| Automated bin packing | Optimizes resource utilization by mixing workloads. |
| Batch execution | Handles batch jobs and CI workloads; replaces failed containers if configured. |
| Cloud Controller Manager | Cloud-provider-specific control logic for Kubernetes. |
| Cluster | A group of worker nodes running containerized applications. |
| Container Orchestration | Automating deployment, scaling, and management of containers. |
| Container Runtime | Software that runs and manages containers. |
| Control Loop | A continuous loop ensuring the actual state matches the desired state. |
| Control plane | Components that expose the API and orchestrate containers. |
| Controller | Control loops that modify cluster state to match desired configuration. |
| Data (Worker) Plane | Provides CPU, memory, networking, and storage to run containers. |
| DaemonSet | Ensures a pod runs on all or selected nodes (e.g., log collectors). |
| Declarative Management | Defining desired state; Kubernetes ensures convergence. |
| Deployment | Manages pods, rollouts, scaling, and rollback. |
| Designed for extensibility | Kubernetes can be extended without modifying core code. |
| Docker Swarm | Docker's orchestrator for containerized apps. |
| Ecosystem | Tools, services, and integrations available around Kubernetes. |
| etcd | Reliable distributed key-value store holding cluster state. |
| Eviction | Removing pods from nodes (e.g., resource pressure). |
| Imperative commands | Direct commands to modify live objects. |
| Imperative Management | Step-by-step instructions to reach desired state. |
| Ingress | Manages external access to cluster services. |
| IPv4/IPv6 dual stack | Allows pods/services to use both IPv4 and IPv6. |
| Job | Runs a finite task to completion. |
| Kubectl | CLI tool for Kubernetes. |
| Kubelet | Node-level agent running containers and reporting status. |
| Kubernetes | Leading open-source container orchestration platform. |
| Kubernetes API | REST API used to interact with and automate the cluster. |
| Kubernetes API Server | Validates and exposes Kubernetes APIs. |
| Kubernetes Controller Manager | Runs all built-in controller processes. |
| Kubernetes Cloud Controller Manager | Cloud-provider-specific control logic. |
| Kubernetes Proxy | Manages network rules and pod-to-service communication. |
| kube-scheduler | Assigns pods to nodes. |
| Label Selector | Selects resources based on labels. |
| Labels | Key/value metadata for grouping Kubernetes objects. |
| Load balancing | Distributing traffic among pods. |
| Marathon | Mesos framework for container orchestration. |
| Namespace | Virtual cluster within a cluster for resource isolation. |
| Node | Worker machine that runs workloads. |
| Nomad | HashiCorp cluster scheduler supporting containers. |
| Object | Kubernetes resource representation. |
| Persistence | Resource existence until explicitly changed or removed. |
| Preemption | Evicting low-priority pods to schedule high-priority pods. |
| Pod | Smallest execution unit; one or more tightly coupled containers. |
| Proxy | Intermediary server for routing requests. |
| ReplicaSet | Ensures a specified number of identical pods are running. |
| Self-healing | Kubernetes restarts or replaces crashed containers. |
| Service | Stable network identity exposing a group of pods. |
| Service Discovery | Finding services/pods by name or IP. |
| StatefulSet | Manages stateful workloads requiring stability and ordering. |
| Storage | Mechanisms for persistent and ephemeral pod storage. |
| Storage Orchestration | Automated mounting of storage to pods. |
| Workload | Any running Kubernetes application. |

### Managing Applications with Kubernetes

| Term | Definition |
|------|------------|
| Cluster Autoscaler | Automatically adjusts the number of nodes in the cluster. |
| Config Map | Stores configuration data separate from container images. |
| Horizontal Pod Autoscaler | Automatically scales pod replicas based on metrics. |
| IBM Cloud catalog | Collection of IBM cloud services. |
| Linguistic Analysis | Detects tone in text. |
| Persistent Volume | Abstraction for persistent storage independent of pod lifecycle. |
| Persistent Volume Claim | Requests persistent storage for a pod. |
| Rolling Updates | Controlled rollout of application changes with rollback support. |
| Secrets | Secure storage for sensitive data like tokens and passwords. |
| Service binding | Connecting applications to backing services (DBs, APIs). |
| Tone Analyzer Service | IBM service using linguistic analysis. |
| Vertical Pod Autoscaler | Automatically adjusts CPU/memory of running pods. |
| Volume | Directory accessible to containers in a pod. |
| Volume Mount | Mounting a volume into a container. |
| Volume Plugin | Integrates storage into pods. |

### OpenShift Basics

| Term | Definition |
|------|------------|
| A/B testing | Testing two versions of an application with users. |
| Build | Transformation of source into an output artifact. |
| BuildConfig | OpenShift object defining how builds run. |
| Canary Deployments | Gradual rollout of new app versions to users. |
| Circuit breaking | Preventing cascading microservice failures. |
| Configuration Change | Build trigger when BuildConfig changes. |
| Control Plane | Defines system configuration and programs proxies. |
| Custom build strategy | Using custom builder images in OpenShift. |
| Custom builder images | Docker images containing logic to build outputs. |
| CRDs | Custom resource types added to the Kubernetes API. |
| Custom controllers | Reconcile CRD state to desired state. |
| Data plane | Handles service-to-service communication. |
| Enforceability | Istio enforces policies to control resource usage. |
| Envoy proxy | Proxy intercepting all service mesh traffic. |
| Human operators | People who deploy and troubleshoot systems. |
| Image Change | Trigger to rebuild on updated base images. |
| ImageStream | Abstraction for tracking image versions. |
| ImageStream Tag | Named pointer to a specific image. |
| Istio | Service mesh enabling security, routing, and observability. |
| Man-in-the-middle attacks | Attacker intercepts communication between two parties. |
| Observability | Monitoring traffic, calls, latency, and errors. |
| OpenShift | Enterprise Kubernetes platform by Red Hat. |
| OpenShift CI/CD process | Automated code merge, build, test, and deploy pipeline. |
| Operators | Software that automates complex cluster tasks. |
| Operator Framework | Tools to build, test, and deliver Operators. |
| OperatorHub | Console for installing Operators. |
| Operator Lifecycle Manager | Manages Operator installation, upgrades, and RBAC. |
| Operator maturity model | Stages from simple install to auto-pilot operations. |
| Operator Pattern | Controller tied to custom resources. |
| Operator Registry | Stores CRDs, CSVs, and Operator metadata. |
| Operator SDK | Toolkit for building Operators (Go, Helm, Ansible). |
| postCommit | Optional build hook. |
| Retries | Reattempt failed microservice requests. |
| runPolicy | Controls how builds run (serial or parallel). |
| Service Broker | Provides short-lived service processes without long-term ops. |
| Service Mesh | Secure and observable service-to-service communication. |
| Software operators | Automated equivalents of human operator tasks. |
| Source-to-Image (S2i) | Builds images by injecting source code into base images. |
| Source strategy | Defines how builds run (Source, Docker, Custom). |
| Source type | Determines input source (Git repo, inline Dockerfile, etc.). |
| Webhook | Build trigger sent from GitHub or other external systems. |

---

### Certificate

[Course certificate](https://www.coursera.org/account/accomplishments/verify/8WPT3TKGBFN2)
